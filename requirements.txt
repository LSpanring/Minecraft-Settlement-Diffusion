# Requirements for the diffusion module.
# Extends ml/requirements-altered.txt with diffusion-specific additions.
# Install with: pip install -r requirements.txt

# ── Core ───────────────────────────────────────────────────────────────────
numpy==1.23.5
PyYAML==6.0.3
tqdm==4.64.1
einops>=0.6.0          # Tensor reshaping utilities used in the U-Net
scipy>=1.10             # Connected-component labeling for compute_connectivity_weight

# ── PyTorch (CUDA 12.8 nightly — same as ml/requirements-altered.txt) ─────
--pre
--index-url https://download.pytorch.org/whl/nightly/cu128
torch
torchvision
torchaudio
--index-url https://pypi.org/simple/

# ── Existing project dependencies (data pipeline, block embeddings) ────────
amulet-core==1.9.11
click==8.1.3
cloup==2.0.0.post1
scikit-learn==1.2.2
termcolor==2.1.1
setuptools>=65,<81
wheel

# ── Experiment tracking and visualisation ─────────────────────────────────
wandb>=0.16,<1
matplotlib==3.7.1
umap-learn==0.5.3
optuna>=4.0
